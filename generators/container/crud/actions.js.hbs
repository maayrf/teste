import {
  LOAD_{{ constantCase name }}S,
  LOAD_{{ constantCase name }}S_ERROR,
  LOAD_{{ constantCase name }}S_SUCCESS,
  REMOVE_{{ constantCase name }},
  REMOVE_{{ constantCase name }}_ERROR,
  REMOVE_{{ constantCase name }}_SUCCESS,
  CREATE_{{ constantCase name }},
  CREATE_{{ constantCase name }}_ERROR,
  CREATE_{{ constantCase name }}_SUCCESS,
  EDIT_{{ constantCase name }},
  EDIT_{{ constantCase name }}_ERROR,
  EDIT_{{ constantCase name }}_SUCCESS,
} from './constants';

// {{ constantCase name }} LOAD

export function load{{ properCase name }}s() {
  return {
    type: LOAD_{{ constantCase name }}S,
  };
}

export function {{ camelCase name }}sLoaded({{ camelCase name }}s) {
  return {
    type: LOAD_{{ constantCase name }}S_SUCCESS,
    {{ camelCase name }}s,
  };
}

export function load{{ properCase name }}sError(error) {
  return {
    type: LOAD_{{ constantCase name }}S_ERROR,
    error,
  };
}
// END {{ constantCase name }} LOAD

// CREATE {{ constantCase name }}
export function create{{ properCase name }}({{ camelCase name }}, resolve, reject) {
  return {
    type: CREATE_{{ constantCase name }},
    {{ camelCase name }},
    resolve,
    reject,
  };
}

export function create{{ properCase name }}Error(error) {
  return {
    type: CREATE_{{ constantCase name }}_ERROR,
    error,
  };
}

export function {{ camelCase name }}Created({{ camelCase name }}) {
  return {
    type: CREATE_{{ constantCase name }}_SUCCESS,
    {{ camelCase name }},
  };
}
// END CREATE {{ constantCase name }}

// EDIT {{ constantCase name }}
export function edit{{ properCase name }}({{ camelCase name }}, resolve, reject) {
  return {
    type: EDIT_{{ constantCase name }},
    {{ camelCase name }},
    resolve,
    reject,
  };
}

export function edit{{ properCase name }}Error(error) {
  return {
    type: EDIT_{{ constantCase name }}_ERROR,
    error,
  };
}

export function {{ camelCase name }}Edited({{ camelCase name }}) {
  return {
    type: EDIT_{{ constantCase name }}_SUCCESS,
    {{ camelCase name }},
  };
}
// END EDIT {{ constantCase name }}

// REMOVE {{ constantCase name }}
export function remove{{ properCase name }}(id) {
  return {
    type: REMOVE_{{ constantCase name }},
    id,
  };
}

export function remove{{ properCase name }}Error(error) {
  return {
    type: REMOVE_{{ constantCase name }}_ERROR,
    error,
  };
}

export function {{ camelCase name }}Removed(id) {
  return {
    type: REMOVE_{{ constantCase name }}_SUCCESS,
    id,
  };
}

// END REMOVE {{ constantCase name }}
