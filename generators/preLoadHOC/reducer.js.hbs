import { fromJS } from 'immutable';
import {
  LOAD_{{ constantCase name }}_BY_ID,
  LOAD_{{ constantCase name }}_BY_ID_ERROR,
  LOAD_{{ constantCase name }}_BY_ID_SUCCESS,
} from './constants';

const initialState = fromJS({
  loading: false,
  error: null,
  success: null,
});

function {{ camelCase name }}Reducer(state = initialState, action) {
  switch (action.type) {
    case LOAD_{{ constantCase name }}_BY_ID:
      return state
        .set('loading', true)
        .set('success', null)
        .set('error', null);
    case LOAD_{{ constantCase name }}_BY_ID_ERROR:
      return state
        .set('loading', false)
        .set('success', null)
        .set('error', action.error);
    case LOAD_{{ constantCase name }}_BY_ID_SUCCESS:
      return state
        .set('loading', false)
        .set('success', {
          {{ camelCase name }}: action.{{ camelCase name }},
          message: 'Pego com sucesso!',
        })
        .set('error', null);
    default:
      return state;
  }
}

export default {{ camelCase name }}Reducer;
